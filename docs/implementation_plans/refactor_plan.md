# Refactor Plan

**Status:** All items complete.

Generated by scanning the `job_search_tool` codebase after Prompts 21–24.

---

## Completed Items

### Prompts 31–38 (Prioritized Refactor List)

| Prompt | Finding |
|--------|---------|
| 31 | Extract `_load_prompt` to shared helper |
| 32 | Extract resume parse + error handling in CLI |
| 33 | Extract `_run_chain_with_spinner` in CLI |
| 34 | Break `apply()` into sub-functions |
| 35 | Extract `_tracker_delete` / `_tracker_edit` |
| 36 | Standardize `load_workbook` read-only usage |
| 37 | Extract shared `run_apply_pipeline` |
| 38 | Shared analysis display formatter |

### Post-Prompts 38 (Optional / Nice-to-Have)

| Item | What was done |
|------|---------------|
| 3.3 Console singleton | Created `utils/console.py` with a module-level singleton. Updated `utils/tracker.py`, `utils/resume_parser.py`, and `config.py` to use it. |
| 6.1 Shared resume parsing helper | Removed redundant `config.RESUME_PATH.exists()` pre-checks from `main.py` and `app.py`. Both now rely on `get_resume_text()` which already raises uniform `FileNotFoundError` / `ValueError`. |

Also completed: error handling style standardization (3.2), `_display_analysis()` table extraction (2.3).
