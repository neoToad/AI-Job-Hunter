# Refactor Plan

Generated by scanning the `job_search_tool` codebase after Prompts 21–24.

---

## Remaining Items (Optional / Nice-to-Have)

### 3.3 Console instantiation
- **Files:** `utils/tracker.py:15` imports `Console` but only `show_tracker` uses it. `utils/resume_parser.py:15` creates `_console` at module level. `config.py:16` imports `Console`.
- **Problem:** Multiple modules create or import `Console` independently.
- **Fix:** Consider a shared `utils/console.py` with a singleton console if more modules need it.
- **Complexity:** Low
- **Status:** Not done — explicitly noted as non-critical in original audit.

### 6.1 Shared resume parsing helper
- **Files:** `main.py` and `app.py` both call `parse_resume(config.RESUME_PATH)` with similar error handling.
- **Problem:** Resume parsing logic is duplicated between CLI and Streamlit entry points.
- **Fix:** Extract a shared `_get_resume_text()` helper that returns the resume string or raises a uniform exception. Both CLI and Streamlit can call it.
- **Complexity:** Low
- **Status:** Not done — was never added to the Prioritized Refactor List table.

---

## Completed Items

All items from the original Prioritized Refactor List have been completed across Prompts 31–38:

| Prompt | Finding |
|--------|---------|
| 31 | Extract `_load_prompt` to shared helper |
| 32 | Extract resume parse + error handling in CLI |
| 33 | Extract `_run_chain_with_spinner` in CLI |
| 34 | Break `apply()` into sub-functions |
| 35 | Extract `_tracker_delete` / `_tracker_edit` |
| 36 | Standardize `load_workbook` read-only usage |
| 37 | Extract shared `run_apply_pipeline` |
| 38 | Shared analysis display formatter |

Also completed: error handling style standardization (3.2), `_display_analysis()` table extraction (2.3).
